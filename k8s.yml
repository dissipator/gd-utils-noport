apiVersion: apps/v1
kind: Deployment
metadata:
  name: gdbot3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gdbot
  template:
    metadata:
      labels:
        app: gdbot
    spec:
      containers:
      - image: registry.cloud.okteto.net/dissipator/dev:okteto 
        name: dev
